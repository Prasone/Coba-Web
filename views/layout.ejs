<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/layout.css">
</head>
<body>
    <header>
        <% if (!user || user.role !== 'admin') { %>
            <nav class="navbar navbar-light bg-light">
                <div class="container">
                    <a class="navbar-brand" href="#">
                        <img src="images/logo1.png" alt="Logo" width="40" height="34">
                    </a>
                    <a href="/">Home</a>
                    <a href="/movies">Movies</a> <!-- Link to /movies route -->
                    <a href="/schedule">Schedule</a> <!-- Link to /schedule route -->
                    <% if (user) { %>
                        <a href="/logout">Logout</a>
                    <% } else { %>
                        <a href="/login">Login</a>
                    <% } %>
                </div>
            </nav>
        <% } %>
    </header>
    <main>
        <%- body %> <!-- The body of the specific page will be rendered here -->
    </main>
    <footer>
        <p>&copy; 2024 Cinema Web. All rights reserved.</p>
    </footer>
</body>
</html>
